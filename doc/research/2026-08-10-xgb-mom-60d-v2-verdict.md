# xgb_mom_60d v2 — the admissible execution: KILL by one fold, with a real and recency-loaded signal

The single execution of the merged v2 prereg (model#213), run 2026-08-10
UTC immediately post-merge with the committed harness. All integrity
duties held: embargoed folds with per-row purge ACTIVE (0 rows purged —
the 91-day gaps already clear every realized label endpoint, now proven
per-row rather than assumed), corpus sha asserted pre-read, features
sha256 stamped, controls regenerated and green, machine-surface verifier
green on the result `[VERIFIED — verifier exit 0]`.

## The gate `[VERIFIED — committed result JSON]`

| leg | bar | measured | result |
|---|---|---|---|
| 1 mean real signal | > 0 | **+0.0257** | PASS |
| 2 fold consistency | ≥6 of the fixed 8 | **5/8** | **FAIL** |
| 3 A/A stability | seed std ≤ 0.01 | 0.0007 | PASS |
| 4 recency | — | 2025 +0.0849, 2026 +0.0911 | PASS |

**Gate arithmetic: KILL** (admissible_verdict remains null in the JSON
until review countersigns, per the frozen protocol). Real signal per fold
(real − shuffle): −0.041 / **+0.089 (2020)** / −0.053 / +0.000 (2022, a
hair above zero) / **+0.066 (2023)** / −0.032 / **+0.085 (2025)** /
**+0.091 (2026)**.

## Prediction scorecard (the pre-run bet, settled honestly)

Recorded before the run: "KILL or marginal PASS; weaker after leak
removal is the expected direction." **Direction right — KILL by one
fold. Magnitude WRONG**: the clean signal is HIGHER than the leaky v1
diagnostic (+0.0257 vs +0.0221). No causal story is attached: v1 and v2
differ in embargo, train spans, test starts, and realized samples
simultaneously, so the comparison settles my prediction and nothing
more.

## Honest reading

The learned momentum form has REAL, verifier-clean average signal that is
strongly RECENCY-LOADED (2023/2025/2026 positive and large; 2019/2021/2024
negative). The frozen bar demanded 6-of-8 year-in-year-out persistence
for a STANDING arm and the model missed it by exactly one fold. This is a
completed outcome under this prereg: no gate moves, no retry inside it.

## What this sharpens (hypothesis only, a NEW dated prereg if pursued)

What is OBSERVED is a recent concentration: positive in 4 of the last 5
folds (>+0.06 in three). Whether that reflects a dispersion/volatility
REGIME mechanism — as opposed to secular drift, sample luck across eight
yearly folds, or anything else — is exactly what a conditional-activation
prereg would have to TEST, not assume. Such a design would freeze:
the activation feature, its threshold, and the same embargoed-fold
discipline, and it inherits the #937/#931 serving gates unchanged.

## Standing consequences

No arm enters the system from this line. The operator's momentum model
question is now answered by an ADMISSIBLE record: real signal, episodic
persistence, one fold short of the standing bar — and the strongest
recent-year profile in the family.
