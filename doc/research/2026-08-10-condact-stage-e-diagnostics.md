# Conditional activation, Stage E diagnostics — the daily dispersion gate finds NO support

The one Stage-E run (merged harness model#216+#217, executed 2026-08-10
immediately post-merge). STAGE E CARRIES NO VERDICT AUTHORITY (model#215
§2b); this is hypothesis-refinement diagnostics, stage-stamped, and the
frozen Stage-C clock is UNCHANGED by anything here.

## The numbers `[VERIFIED — committed stage-stamped artifact; condact verifier exit 0]`

| quantity | value |
|---|---|
| A=1 (high-dispersion) days / A=0 days | 690 / 667 (guards met; A flips in all 8 folds) |
| mean daily real signal, A=1 | **+0.0151** |
| mean daily real signal, A=0 | **+0.0202** |
| A=1 bootstrap 95% CI | [−0.030, +0.054] — includes 0 |
| A=1 − A=0 contrast CI | [−0.051, +0.035] — includes 0, point estimate NEGATIVE |
| gate arithmetic (no authority) | KILL-shaped (gates 1 and 2 fail) |

## Honest reading

1. **The first operationalization of "行情年选手" does not survive daily
   resolution**: the momentum model's signal is NOT concentrated on
   high-dispersion days — if anything the point estimate leans the other
   way. The yearly fold concentration (2020/2025/2026, model#214) is not
   a daily ROC20-dispersion phenomenon.
2. **The two-stage discipline just paid for itself**: had E carried
   verdict power, this negative would end the line noisily; as
   diagnostics it cleanly says "wrong condition variable or wrong
   timescale" while the frozen Stage-C rule marches to its own clock
   (and, on this evidence, most likely to a KILL — expectation recorded
   now).
3. **The surviving hypothesis space narrows usefully**: the yearly
   concentration may live at REGIME timescale (months), not daily state;
   or in a different variable (index-level vol, breadth, rate regime).
   Each is a NEW dated prereg if pursued; none may inherit this run's
   data for confirmation.

## What this changes operationally

Nothing is deployed, nothing was going to be. The MoE condition axis
stays open scientifically but has spent its cheapest bullet; the
first-iteration effect surface (operator directive 2026-08-10) remains
L1/L2 shadows + the serving-gap chain, which do not depend on this line.
