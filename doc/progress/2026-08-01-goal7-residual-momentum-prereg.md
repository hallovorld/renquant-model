# Progress: the momentum freeze candidate (GOAL-7)

WHAT: `doc/research/2026-08-01-goal7-residual-momentum-prereg.md` — the preregistration
that #161 (approved design) and #162 (approved inference protocol) both point at. Frozen
on merge. Resolves everything they left open: L=59 with a precommitted worst-case bar
over the frozen two-source generator family (label overlap MA(19) by construction +
AR-fitted persistence), α=0.05 split across the two decision tests, the AR-adequacy
collapse rule fixed pre-run, MDE ceiling 0.06 for UNRESOLVED-POWER, the 43 non-payer
assumption frozen with its against-the-candidate bias direction, and the v1-v2-style
execution gate (single run, verbatim committed output, deviations VOID).

DISCIPLINE: no IC/score/label statistic computed; runner is a separate post-freeze PR;
KILL and UNRESOLVED carry equal reporting prominence.
