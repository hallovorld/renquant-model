# Progress: the null-construction protocol both waiting lanes need (discussion)

WHAT: `doc/design/2026-08-01-dependence-matched-null-protocol.md` — the separately
freezable protocol that model#161 §3a′ (momentum H1, UNRESOLVED-METHOD) and model#154
(v1-vs-v2 amendment 2d) both name as their missing instrument. Fit-by-frozen-rule AR(p)
with residual resampling, adequacy-gated against the sample ACF, seeded calibration of a
series-specific bar, three validation gates (positive control / machinery self-check /
mis-specification stress with a both-bars publication rule), and the self-referential
weak point (width estimated from the tested series) named with its bias direction.

DISCIPLINE: discussion only; no real series touched; every constant marked as a proposal
for each consumer's own freeze. Docs only.
