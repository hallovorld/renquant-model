# MoE evidence audit: no alpha claim is justified yet

STATUS: delivered - research audit only; no model, configuration, artifact, or
runtime surface changes.

WHAT: Adds a read-only panel/classifier audit, a committed result JSON, and a
research report that separates the rejected 75/25 slow blend, the unpowered
legacy ensemble screen, and the remaining classifier-tail hypothesis.

WHY/DIR: The operator requested an independent assessment of whether the
revised MoE proposal is scientific, data-supported, and practically actionable.
This keeps the direction falsifiable: candidate diversity is not alpha, a
positive top-three observation without power/costs is not alpha, and an
evaluator that cannot detect a predeclared material effect cannot certify a
blend.

EVIDENCE:
artifact: `doc/research/data/2026-08-08-moe-evidence-audit.json`
prod or exp: experiment
existing data: canonical orchestrator S10 confirmatory kill rejects the frozen
75/25 slow blend; `doc/research/data/2026-07-30-goal4-phase0-ensemble-gain/`
shows the older broad-ensemble evaluator was VOID because its positive control
lacked power at a realistic injected increment.
best-known?: panel champion remains best-known; the audited classifier arm is
worse on the fixed panel-label IC target and the equal-rank blend is not
detectably better on the shared corpus.
scope: this is the panel/classifier corpus audit in experiment space, evaluated
against the panel `fwd_60d_excess` label only; it does not establish
deployability, economics, or promotion.

NEXT: Keep the panel champion. Do not add a sector/regime gate or blend to a
shadow lane until a new evaluator establishes power and a fresh confirmation
clears its economics and IC guards.
